import { ContractFactory, ethers, parseUnits, } from 'ethers';

const tokens = (n: number) => parseUnits(n.toString(), 'ether');

describe('Escrow', () => {
    it('Saves the addresses', async () => {
        const RealEstate = ethers
    })
})
